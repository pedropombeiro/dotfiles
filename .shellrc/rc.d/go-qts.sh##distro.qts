#!/usr/bin/env bash

ASDF_GO="$(asdf where golang)"
if [[ -d ${GOROOT} ]] && [[ -z ${ASDF_GOLANG_DEFAULT_PACKAGES_FILE} ]]; then
  PATH="${PATH}:${ASDF_GO}/go/bin"
  ASDF_GOLANG_DEFAULT_PACKAGES_FILE=~/.default-golang-pkgs

  export ASDF_GOLANG_DEFAULT_PACKAGES_FILE
  export PATH
fi
