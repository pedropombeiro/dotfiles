#!/usr/bin/env zsh

# op completions are pre-generated by 060-generate-completions.zsh into site-functions.
# Just register the compdef (deferred via zvm_after_init to run after zsh-vi-mode).
if (( $+commands[op] )); then
  zvm_after_init_commands+=('compdef _op op')
fi
