
#
# `lg` opens lazygit, invoking 'yadm enter' if needed
#

if [[ "$PWD" == "$HOME" || "$PWD" == "$HOME/.config"* ]]; then
  GIT_DIR="$HOME/.local/share/yadm/repo.git" GIT_WORK_TREE="$HOME" lazygit "$@"
else
  lazygit "$@"
fi
