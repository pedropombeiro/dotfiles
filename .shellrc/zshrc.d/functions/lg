
#
# `lg` opens lazygit, invoking 'yadm enter' if needed
#

if [[ "$PWD" == "$HOME" || "$PWD" == "$HOME/.config"* ]]; then
  yadm enter lazygit --use-config-dir ~/.config/lazygit
else
  lazygit --use-config-dir ~/.config/lazygit
fi
